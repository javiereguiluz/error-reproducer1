Run this to trigger an exception in the console:

```
$ cd reproducer1/
$ composer install
$ php bin/console
```

You should see the new error design:

![](/new-symfony-error.png)

Instead of the original error design:

![](/original-symfony-error.png)
